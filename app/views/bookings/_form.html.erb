<%= simple_form_for (new_space_booking_path) do |f| %>

  <div class="container mt-3">
    <%= f.datetime_select :from %>
  </div>
  <div class="container mt-3">
    <%= f.datetime_select :to %>
  </div>
  <div class="container mt-3">
    <%= f.submit "Book this space", class: "btn btn-primary" %>
  </div>
<% end %>
